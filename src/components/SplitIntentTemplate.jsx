import React from 'react';

export default function SplitIntentTemplate({ splitMode }) {
  return (
    <div style={{ padding: '2rem', border: '1px solid #ddd', borderRadius: '8px' }}>
      <h2>Split Intent: {splitMode}</h2>
      <p>(Split Component Stub)</p>
    </div>
  );
}
